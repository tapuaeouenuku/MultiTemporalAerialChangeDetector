# MultiTemporalAerialChangeDetector
Uses before and after satellite images to detect changes in building presence. Useful for detecting new or removed buildings.

This program has a GUI, data aquisition and automatic training. Easy to use. Currently only aquires helsinki data.
